# ts-rs writes TypeScript bindings here when `cargo test` runs. DEVELOPMENT.md "The command boundary".
[env]
TS_RS_EXPORT_DIR = { value = "../src/ipc/bindings", relative = true }
TS_RS_LARGE_INT = "number"
