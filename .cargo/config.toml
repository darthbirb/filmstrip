# At the repository root, so Cargo finds it from here and from src-tauri alike. ts-rs writes the
# TypeScript bindings when `cargo test` runs. DEVELOPMENT.md "The command boundary".
[env]
TS_RS_EXPORT_DIR = { value = "src/ipc/bindings", relative = true }
TS_RS_LARGE_INT = "number"
